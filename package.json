{
	"name": "multiplehit-api",
	"version": "1.0.0",
	"description": "Proxy untuk ambil data BPJS SIJSTK",
	"main": "api/proxy.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"axios": "^1.7.7"
	}
}
